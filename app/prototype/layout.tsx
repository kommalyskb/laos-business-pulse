export const metadata = {
  title: "ພ້ອມໄປ — Interactive UX Prototype",
  description: "Prototype ສຳລັບທົດສອບ Feed, Search, Place, trust labels ແລະ decision actions.",
};

export default function PrototypeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
